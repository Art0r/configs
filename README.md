# Uso

## Copiando arquivos de configuração do LunarVim

```sh
rm -rf ~/.config/lvim && mkdir -p ~/.config/lvim && cp -r ~/configs/lvim/* ~/.config/lvim/
```

## Ativando ambiente Nix

```sh
nix-shell ~/configs/shell.nix
```
